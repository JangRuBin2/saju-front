# 야간 자동 작업 기록 — 2026-03-04

> 커밋: `ca5661b`
> 빌드: ✅ 성공 (63 페이지 + robots.txt + sitemap.xml)

---

## 작업 요약

5개 병렬 에이전트가 코드만으로 완료 가능한 백로그 항목을 모두 처리했습니다.

---

## 1. ErrorMessage 컴포넌트 통합 (UI-01)

### 변경 파일
- `src/app/[locale]/saju/result/page.tsx`
- `src/app/[locale]/compatibility/page.tsx`
- `src/app/[locale]/today/page.tsx`

### 변경 내용
- 사주 결과 페이지: 인라인 `<p>` 에러 표시 → `<ErrorMessage>` 컴포넌트 + 재시도 콜백 (sessionStorage 재읽기)
- 궁합 페이지: 인라인 에러 div → `<ErrorMessage error={error} onRetry={handleReset} />`
- 오늘의 운세: 에러 상태에 ErrorMessage 추가

---

## 2. 폼 검증 강화 + Auth.js 보안 (UI-03, AUTH-04)

### 변경 파일
- `src/components/saju/SajuForm.tsx`
- `src/lib/auth.ts`
- `messages/{ko,en,ja,zh}.json`

### 변경 내용
- **SajuForm 검증**:
  - 출생 연도 상한값: 하드코딩 `2030` → `new Date().getFullYear()`
  - 연도 범위 검증: 1900 ~ 현재년도 외 입력 시 `validation.yearRange` 에러
  - 월별 일수 검증: `new Date(year, month, 0).getDate()`로 해당 월 최대 일수 확인
  - 4개 언어 번역 키 추가: `yearRange`, `dayInvalid`
- **Auth.js 보안**:
  - `trustHost: true` — 프록시/Vercel 환경 호스트 검증 우회
  - `allowDangerousEmailAccountLinking: true` — 동일 이메일 다중 OAuth 계정 연결

---

## 3. 계정 삭제 + SEO (AUTH-03, OPT-02)

### 변경 파일
- `src/lib/server/profile-actions.ts` (deleteAccount 서버 액션 추가)
- `src/app/[locale]/mypage/page.tsx` (계정 삭제 UI)
- `src/app/[locale]/layout.tsx` (SEO 메타데이터)
- `src/app/robots.ts` (신규)
- `src/app/sitemap.ts` (신규)

### 변경 내용
- **계정 삭제**: `deleteAccount()` 서버 액션 + 마이페이지 하단 "계정 삭제" 버튼 + 확인 다이얼로그 + signOut 후 홈으로 리다이렉트
- **SEO**:
  - layout.tsx: title template, description, keywords, OpenGraph, robots 메타데이터
  - robots.ts: /api/, /mypage/, /dashboard/ 크롤링 차단
  - sitemap.ts: 4개 로케일 × 6개 라우트 = 24 URL 생성

---

## 4. 결제 페이지 i18n 완료 (PAY i18n)

### 변경 파일
- `src/app/[locale]/payment/page.tsx`
- `messages/{ko,en,ja,zh}.json`

### 변경 내용
- 하드코딩 한국어 텍스트를 모두 `t()` 번역 키로 전환
- 19개 새 번역 키 추가 (4개 언어):
  - `free4`, `free5` (무료 플랜 제한 기능)
  - `premium4`, `premium5` (프리미엄 추가 기능)
  - `currentlyUsing`, `discount`, `socialProof`, `rating`
  - `faq`, `faqQ1~3`, `faqA1~3`
  - `perMonth`, `perYear`, `monthlyEquiv`, `best`

---

## 5. 프로필 저장 UX + 구독 페이지 (UI-02, PAY-03)

### 변경 파일
- `src/app/[locale]/mypage/profile/page.tsx`
- `src/app/[locale]/mypage/subscription/page.tsx`

### 변경 내용
- **프로필 저장**: GoldButton에 `loading` prop 전달, 에러 처리, 성공 시 ✓ 피드백
- **구독 페이지 리뉴얼**:
  - 프리미엄 사용자: 구독 만료일 + 잔여 일수 표시
  - 기능 체크리스트 UI (프리미엄 포함 기능 목록)
  - LoadingBook 로딩 상태

---

## 빌드 수정 사항

- `monthlyEquivalent` null 타입 에러: `?? ""` 폴백 추가 (payment/page.tsx:265)

---

## 남은 작업 (코드만으로 불가)

| 항목 | 이유 |
|------|------|
| Phase 0 인프라 | 환경변수, DB, OAuth 키 등 수동 설정 필요 |
| Phase 1 API 테스트 | 백엔드 서버 실행 필요 |
| Phase 4 결제 에러 처리 | 토스 API 응답 구조 확인 필요 |
| Phase 6 배포 | Vercel 설정, 도메인 등 |
| Phase 7 PWA/공유 | 이미지 에셋, 서비스 워커 등 |
